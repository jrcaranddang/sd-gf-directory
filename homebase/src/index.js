// Entry point: initializes storage and starts the Telegram bot

// TODO: wire up agents and bot in v1.1
// This will import startBot from ./telegram/bot.js and begin listening for messages.

console.log('HomeBase v0.1.0 — run `node --test tests/` to verify the ingestion agent');
